@Library('build-pipeline-emilio@build-spec') _
import org.foo.*

standardBuildSpec {
    dotnetlib {
        projectName = 'Traffic.Api.Model'
        projectDir = 'a/b/c'
    }
    
    dotnetapi {
        projectName = 'Traffic.Api'
        projectDir = 'a/b/c'
    }
}
